export const styles = ({ spacing }) => ({
    message: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        padding: spacing(1, 0),
    },
    messageText: {
        marginLeft: spacing(2),
    },
});
